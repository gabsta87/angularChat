import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ch.gabrielmaret.angularchat',
  appName: 'angularChat',
  webDir: 'dist/angular-chat',
  bundledWebRuntime: false
};

export default config;
